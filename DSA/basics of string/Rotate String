class Solution:    
    def rotateString(self, s, goal):
        #your code goes here
        if goal==s:
            return True
        for i in range(len(s)):
            rotated=s[i:]+s[:i]
            if rotated==goal:
                return True
        return False

        
      
        # first=s[0]
        # last=s[-1]
        # ndfirst=0
        # ndlast=0
        # if goal==s:
        #     return True
        # for i in range(len(goal)):
        #     if goal[i]==first:
        #         ndfirst=i
        #     if goal[i]==last:
        #         ndlast=i
        # print(goal[ndfirst:]+goal[:ndlast+1])
        # if goal[ndfirst:]+goal[:ndlast+1]==s:
        #     return True
        # else:
        #     return False